{
  "name": "aws-lambda-crud",
  "version": "1.0.0",
  "description": "AWS lambda CRUD App",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.395.0",
    "@aws-sdk/util-dynamodb": "^3.395.0",
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
